# .env.local 파일을 생성하고 아래 변수를 본인 환경에 맞게 채워주세요
VITE_API_BASE_URL=http://localhost:8080

# true로 설정 시 소셜 로그인 버튼이 실제 OAuth 흐름으로 동작합니다.
# 설정하지 않으면 개발용 mock 토큰으로 로그인됩니다.
VITE_USE_REAL_AUTH=true

# 운영 배포 템플릿: deploy/env.prod.example → deploy/.env (AWS EC2/S3 배포 시)