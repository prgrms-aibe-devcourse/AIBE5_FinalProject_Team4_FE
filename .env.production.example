# 운영 빌드용 Vite 환경 변수 템플릿
# 사용: cp .env.production.example .env.production 후 값 입력 → npm run build
# 또는 deploy/scripts/build.sh (deploy/.env 사용) 권장

# BE 공개 URL (HTTPS, trailing slash 없음)
VITE_API_BASE_URL=

# 운영 OAuth
VITE_USE_REAL_AUTH=true
